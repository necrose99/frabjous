# config file for /etc/init.d/prometheus-pushgateway
# run /usr/bin/pushgateway -h to see available options

PUSHGATEWAY_DATADIR="/var/lib/pushgateway"
PUSHGATEWAY_OPTS="-persistence.interval="${PUSHGATEWAY_DATADIR}/persistence.data""
